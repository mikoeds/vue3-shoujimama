export interface categoryInfoType {
    id:number,
    image:string,
    is_vajra:number,
    name:string,
    sort:number,
    status:number
}

export interface categoryParamType {
    name:string
}
